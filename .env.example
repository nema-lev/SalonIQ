# Локална разработка — docker-compose
# НЕ commit-вай в git!

# PostgreSQL
POSTGRES_PASSWORD=dev_password_local_only

# JWT (за dev — не се ползва в prod)
JWT_SECRET=dev_jwt_secret_min_64_chars_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
JWT_REFRESH_SECRET=dev_refresh_secret_min_64_chars_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# App
APP_DOMAIN=saloniq.bg
FRONTEND_URL=http://localhost:3000
CORS_ORIGINS=http://localhost:3000
INTERNAL_API_KEY=dev_internal_key_12345

# Node env
NODE_ENV=development
